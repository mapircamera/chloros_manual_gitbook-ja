# プロジェクト設定

Chloros

のサイドバーにある「プロジェクト設定」（<img src="../.gitbook/assets/icon_project-settings.JPG" alt="" data-size="line">

）では、画像処理、キャリブレーションターゲットの検出、マルチスペクトル指数の計算、およびプロジェクトのエクスポートオプションなど、プロジェクトに関するあらゆる設定を行うことができます。これらの設定はプロジェクトとともに保存され、テンプレートとして保存して複数のプロジェクトで再利用することも可能です。

## プロジェクト設定へのアクセス

プロジェクト設定にアクセスするには：

1.Chloros

でプロジェクトを開きます
2. 左側のサイドバーにある **プロジェクト設定**<img src="../.gitbook/assets/icon_project-settings.JPG" alt="" data-size="line">

タブをクリックします
3. 設定パネルに、カテゴリ別に整理された利用可能なすべての設定オプションが表示されます



<!-- SCREENSHOT-NEEDED: Full Project Settings sidebar of a LATTICE project, scrolled so the Processing category is visible showing the per-product export checkboxes (Export sensor response, Export vignette corrected, Export debayered, Export preview, Export radiance, Export reflectance) and the Debayer method row. -->

{% hint style="info" %}
**他の設定に依存する設定はグレー表示になります。** 親となるスイッチの設定によって特定の設定が不可能になる場合（例：*反射率キャリブレーション / ホワイトバランス* のチェックを外すと *反射率のエクスポート* が不可能になる）、依存するコントロールは無効化され、そのツールチップには変更が必要なスイッチの名前が表示されます。
{% endhint %}

***

## 表示

### 画像サムネイルの解像度

* **タイプ**: ドロップダウン選択
* **オプション**: `Default (512 px)`、`1024 px`、`2048 px`、`Full resolution`
* **デフォルト**: デフォルト (512 px)
* **説明**: 画像グリッドのサムネイルがレンダリングされる解像度（最も長い辺、ピクセル単位）。 値が大きいほど、ズームインした際に鮮明に見えますが、読み込みに時間がかかり、メモリを多く消費します。フル解像度を選択すると、元の画像サイズで表示されます。
* **注**: 表示のみ — 処理やエクスポートされたファイルには一切影響しません。***

## ターゲット検出

これらの設定は、Chloros

が画像内のキャリブレーションターゲットをどのように検出して処理するかを制御します。 どちらも、****反射率キャリブレーション / ホワイトバランス**が有効になっている間のみ有効です（それ以外の場合は、ターゲット検出が完全にスキップされるため、グレーアウトされます）。

### 最小キャリブレーションサンプル領域（px）

* **タイプ**：数値
* **範囲**：0～10,000ピクセル
* **デフォルト**: 25 ピクセル
* **説明**: 検出された領域が有効なキャリブレーションターゲットサンプルとみなされるために必要な最小面積（ピクセル単位）を設定します。値を小さくすると、より小さなターゲットを検出できますが、誤検出が増える可能性があります。値を大きくすると、検出にはより大きく、より鮮明なターゲット領域が必要になります。
* **調整すべき場合**:
  * 画像の小さなアーティファクトで誤検出が発生する場合は、この値を大きくしてください
  * 画像内のキャリブレーションターゲットが小さく、検出されていない場合は、この値を小さくしてください

### 最小ターゲットクラスタリング (0～100)

* **タイプ**: 数値
* **範囲**: 0～100
* **デフォルト**: 60
* **説明**: キャリブレーションターゲットを検出する際、類似した色の領域をグループ化するクラスタリングの閾値を制御します。値が大きいほど、グループ化される類似色の数が多くなり、より保守的なターゲット検出となります。値が小さいほど、ターゲットグループ内の色のばらつきが許容されます。
* **調整すべき場合**:
  * キャリブレーションターゲットが複数の検出に分割されている場合は、値を大きくしてください
  * 色のばらつきがあるキャリブレーションターゲットが完全に検出されていない場合は、値小さくしてください

***

## 処理

これらの設定は、Chloros

が画像をどのように処理し、キャリブレーションを行うかを制御します。

### ヴィネット補正

* **タイプ**: チェックボックス
* **デフォルト**: 有効（チェック入り）
* **説明**: 画像の端でのレンズの暗さを補正するために、ヴィネット補正を適用します。ヴィネットとは、レンズの特性により、画像の隅や端が中央よりも暗く見える一般的な光学現象です。
* **副作用**: このスイッチは、実行時に書き出される *未校正のフォールバック製品* の選択にも影響します（以下を参照）。

### 反射率校正 / ホワイトバランス

* **タイプ**: チェックボックス
* **デフォルト**: 有効（チェック済み）
* **説明**: 反射率キャリブレーションを有効にします。カメラの種類や利用可能なデータに応じて、フレーム内で検出されたキャリブレーションターゲットおよび／または DAQ 光センサーのダウンウェルデータに基づいて行われます。これにより、データセット全体の反射率値が正規化され、照明条件にかかわらず一貫した測定値が得られます。
* **無効の場合**：ターゲット検出は完全にスキップされ、**どのカメラでも反射率プロダクトを生成できません** — ターゲット駆動型のSurvey3

も、DAQ駆動型のLATTICEも同様です。関連する設定（*反射率のエクスポート*、*最小再校正間隔*、およびターゲット検出の閾値）はグレーアウトされます。

### 未校正のフォールバックデータ：センサー応答のエクスポート／ヴィネット補正済みのエクスポート

* **タイプ**：2つのチェックボックス
* **デフォルト**：両方とも有効 （チェック済み）
* **説明**：フレームの反射率キャリブレーションができない場合（キャリブレーションターゲットが見つからない、または反射率キャリブレーションがオフになっている）、そのフレームは代わりに *未キャリブレーションのフォールバック製品* として書き出されます。 **各カメラモデルごとに、1回の実行につき2つのフォールバック製品のうち正確に1つが存在し**、*ヴィネット補正*スイッチによって選択されます：
  * ヴィネット補正 **オン**→ `Vignette_Corrected_Images/`（**ヴィネット補正済みデータをエクスポート**によって制御されます）
  * ヴィネット補正 **オフ**→ `Sensor_Response_Images/`（**「センサー応答をエクスポート」**によって制御されます）
* 適用されていないフォールバックはグレー表示されます。適用中のチェックボックスをオフにすると、そのファイルは一切書き込まれなくなります。

### LATTICE エクスポート製品

LATTICE キャプチャを含むプロジェクトの場合、インポートされた各 LATTICE フレームは、1回の処理パスで、有効化されている**かつ適用可能な**すべての製品にファンアウトされます。4つのチェックボックスでファンアウトを制御します（すべてデフォルトで**オン**）：

| 設定 | 出力フォルダ | エクスポート対象 |
| --- | --- | --- |
| **デベイヤー処理済み画像のエクスポート** | `Debayered_Images/` | 線形デベイヤー処理済み画像。RGB

およびマルチスペクトルカメラに適用されます。 |
| **プレビューをエクスポート** | `Preview_Images/` | 表示プレビュー。RGB

は、ホワイトバランス（利用可能な場合はDAQ光源、それ以外はグレイワールド）＋ガンマ補正； マルチスペクトル = 偽色伸張。 |
| **放射輝度エクスポート** | `Radiance_Images/` | W/m²/sr/nm単位のFloat32スペクトル放射輝度。マルチスペクトル （M3C/M3M）のみ —RGB

マスターには適用されません。*キャリブレーション済み画像形式*の設定にかかわらず、常に32ビットTIFF

として書き込まれます。 |
| **反射率のエクスポート**| `Reflectance_Calibrated_Images/` | Uint16 反射率。**32768 = 反射率 1.0** となるようスケーリング済み（XMP `Chloros:PixelScale` としてスタンプされる）。マルチスペクトルのみ。対応する`.daq`ダウンウェルリングレコード（またはQAに合格したフレーム内ターゲット）がフレームをカバーしている場合に書き込まれます。 |

*RGB

のマスターカメラは、デベイヤー処理済み＋プレビューを出力します。これらのカメラについては、適用されないため、放射度/反射率はスキップされます。
* デベイヤー処理済み／プレビュー画像のビット深度は、「*校正済み画像フォーマット*」設定に従う。放射輝度は常にfloat32となる。
*Survey3

処理は、これら4つのトグルの影響を受けない。

同じ4つのトグルは、ヘッドレスモードでは`chloros-cli process --debayered / --preview / --radiance / --reflectance`として、またSDK

の対応するパラメータとしても存在します。これらは、現在は存在しない旧フラグ`--radiometric-output`に代わるものです。

{% hint style="warning" %}
**該当するすべてのプロダクトを無効にすると、実行は失敗します。** バージョン 1.2.0 以降、プロダクトの出力を要求されたにもかかわらず画像プロダクトを書き出さなかった処理実行は、静かに成功と報告されるのではなく、失敗として報告され、CLI

はゼロ以外の値で終了します。ログには、書き出せなかったプロダクトとその理由が記載されます。意図的にメタデータのみを扱う実行 （要求なし）は、引き続き成功とみなされます。
{% endhint %}

### 反射率ソース（プロジェクト設定、CLI

/SDK

経由で設定）

また、このプロジェクトには、LATTICE反射率プロダクトが使用する**反射率基準**も保存されます。設定パネルには専用のコントロールはありません。この値はプロジェクト構成に`Processing → "Target reflectance source"`として保存され、`chloros-cli process --reflectance-source {auto,target,daq}`またはSDK

の`reflectance_source`パラメータを使用して設定します：

* **`auto`**（デフォルト）：QAに合格したフレーム内のキャリブレーションターゲットが絶対基準となります。ターゲットが存在しない場合やQAに失敗した場合は、DAQのダウンウェル分割値（ρ = πL/E）にフォールバックします。
* **`target`**：厳密なターゲット主導の反射率 — DAQによる置換は行われない。
* **`daq`**：DAQを優先する反射率； フレーム内のターゲットは基準として使用されません。

保存された値は大文字小文字を区別せずに照合され、以下のいくつかの表記が別名として受け入れられます：`target`、`target_image`、`empirical`、および `empirical_line` はすべて **ターゲット**を意味します。`daq`、`dls`、`light_sensor`、および `sensor` はすべて**daq**を意味します。それ以外（キーが存在しない場合を含む）はすべて**auto** として扱われます。

単位ごとの **測定** ターゲットスキャンは、ターゲット単位のシリアル番号/QR に基づいて、`<serial>.csv` のように、以下の 3 箇所で参照されます： `--target-reflectance-dir`で指定されたディレクトリ（`Processing → "Target reflectance dir"`として保存）、 プロジェクト固有の `target_reflectance/` フォルダ、および環境変数 `CHLOROS_TARGET_REFLECTANCE_DIR` に指定されたパス。そのユニットに対して測定済みスキャンが存在しない場合、代わりにターゲットモデルの公称公開カーブが使用されます。

### デベイヤー方式

* **タイプ**: ドロップダウン選択
* **オプション**:
  * 標準 (高速、中程度の画質)
  * テクスチャ対応 (低速、最高画質) \[Chloros

+]
* **デフォルト**: 標準 (高速、中程度の画質)
* **説明**: ベイヤー配列センサーの生データをフルカラー画像に変換するために使用するデモザイクアルゴリズムを選択します。「標準（高速、中程度の画質）」方式は、処理速度と画質のバランスが最適です。「テクスチャ対応（低速、最高画質）」 \[Chloros

+]は、高品質なエッジ認識型デベイヤーと、デベイヤー処理によるノイズをほぼ完全に除去するAI/MLノイズ除去モデルを組み合わせています。テクスチャアウェアモデルの実行には、GPUメモリ（VRAM）が必要です。 処理を高速化するため、4GB以上のVRAMが利用可能な場合にこの方法の使用をお勧めします。
* **行がドロップダウンメニューになっている場合**：2つの選択肢からなるドロップダウンは、**両方の条件**が満たされた場合にのみ表示されます。つまり、対象となるChloros

+サブスクリプションでログインしていること、**かつ**プロジェクトにLATTICEキャプチャが含まれていないことです。それ以外の場合、その行は「`Standard (Fast, Medium Quality)`」というプレーンテキストとして表示され、選択可能な項目は表示されません。
* **LATTICEに関する注意**: LATTICE用にトレーニングされたTexture Awareモデルは存在せず、パイプラインは保存された値にかかわらず、LATTICEフレームに対して標準のデモザイク処理を強制します。すでにTexture Awareが選択されているプロジェクトにLATTICEフォルダを追加した場合、Chloros

により、設定は「Standard」に書き換えられ、`project.json`に古い値が残されることはありません。

### 再キャリブレーションの最小間隔

* **タイプ**: 数値
* **範囲**: 0 ～ 3,600 秒
* **デフォルト**: 0 秒
* **説明**: キャリブレーションターゲットを使用する間の最小時間間隔（秒単位）を設定します。0に設定すると、Chloros

は検出されたすべてのキャリブレーションターゲットを使用します。より大きな値に設定すると、Chloros

はこの値以上の間隔を空けて検出されたキャリブレーションターゲットのみを使用するため、キャリブレーションターゲットが頻繁に撮影されるデータセットの処理時間を短縮できます。
* **調整すべき場合**:
  * 照明条件が変動する場合、キャリブレーションの精度を最大化するには 0 に設定してください
  * 照明が一定で、キャリブレーションターゲットの画像が頻繁に取得される場合は、処理を高速化するために値を大きくしてください（例：60～300 秒）

### 光センサーのタイムゾーンオフセット

* **タイプ**: 数値
* **範囲**: -12 ～ +12 時間
* **デフォルト**: 0 時間
* **説明**: 光センサーデータのタイムスタンプに対するタイムゾーンオフセット（UTCからの時間）を指定します。これは、光センサーのログを画像の撮影時刻と照合する際に使用されます。新しい`.daq`の記録には独自のタイムゾーン情報が含まれているため、この設定は主に現地時間で記録された古いログに対して必要となります。

### PPK補正の適用

* **タイプ**: チェックボックス
* **デフォルト**: 無効（チェックなし）
* **説明**: GPS（GNSS）を搭載したMAPIR

製DAQレコーダーからのPPK（Post-Processed Kinematic）補正の使用を有効にします。有効にすると、Chloros

はプロジェクトディレクトリ内にある露光ピンデータを含む.daqログファイルを使用し、画像に正確な位置情報補正を適用します。
* **要件**: プロジェクトディレクトリに、露出ピンエントリを含む .daq ログファイルが存在している必要があります
* **有効にするタイミング**: .daq ログファイルに露出フィードバックエントリが含まれている場合は、常に PPK 補正を有効にすることを推奨します。

### 露出ピン 1

* **タイプ**: ドロップダウン選択
* **表示条件**: 「PPK補正を適用」が有効であり、かつピン 1 の露出データが利用可能な場合にのみ表示されます
* **オプション**:
  * プロジェクト内で検出されたカメラモデル名
  * 「使用しない」 - この露出ピンを無視する
* **デフォルト**: プロジェクトの設定に基づいて自動選択される
* **説明**: PPK 時刻同期のために、露出ピン 1 に特定のカメラを割り当てます。 露出ピンは、カメラのシャッターが切られた正確なタイミングを記録するもので、正確なPPK位置特定に不可欠です。
* **自動選択の動作**:
  * カメラ1台＋ピン1本：カメラが自動的に選択されます
  * カメラ1台＋ピン2本：ピン1が自動的にそのカメラに割り当てられます
  * 複数のカメラ：手動での選択が必要

### 露出ピン 2

* **タイプ**：ドロップダウン選択
* **表示**：「PPK補正を適用」が有効であり、かつピン 2 の露出データが利用可能な場合にのみ表示されます
* **オプション**：
  * プロジェクト内で検出されたカメラモデル名
  * 「使用しない」 - この露出ピンを無視する
* **デフォルト**: プロジェクトの設定に基づいて自動選択される
* **説明**: デュアルカメラ構成を使用する場合、PPK 時刻同期のために特定のカメラを露出ピン 2 に割り当てます。
* **自動選択の動作**:
  * カメラ 1 台 + ピン 1 つ: ピン 2 は自動的に「使用しない」に設定されます
  * カメラ1台＋ピン2本：ピン2は自動的に「使用しない」に設定されます
  * カメラ複数台：手動での選択が必要です
* **注**：同じカメラをピン1とピン2の両方に同時に割り当てることはできません。***

## DAQ光センサー

このセクションは「プロジェクト設定」に表示され、プロジェクト内のすべてのDAQダウンウェルファイル（`.daq`の記録およびDAQ-M `.csv`のダウンウェルログ）が一覧表示されます。「光センサー」タブで作成された記録は、開いているプロジェクトに に自動的に追加されます。



<!-- SCREENSHOT-NEEDED: Project Settings "DAQ Light Sensor" section of a project containing at least one .daq file, showing the "Cap override (all files)" dropdown and a per-file row with its resolved cap. -->

各行には、ファイル、センサーモデル、およびそのファイルに対して実際に適用されているディフューザーキャップ補正が表示されます。行の上部には、プロジェクト全体に適用される単一のコントロールがあります：

### キャップのオーバーライド（すべてのファイル）

* **タイプ**：ドロップダウン選択
* **オプション**：`Auto` および、プロジェクト内に存在するセンサー種に対して有効なキャップ補正プロファイル
* **デフォルト**: 自動
* **保存形式**: `Processing → "DAQ cap id"`（デフォルトは `auto`）
* **説明**: `Auto`は、各ファイルに記録されたキャップを使用します（何も記録されていない場合はサンシャイン・キャップが適用されます。すべてのMAPIR

データ収集装置には、サンシャイン補正機能が標準で搭載されています）。 特定のキャップを選択すると、プロジェクト内の**すべての**ダウンウェルリングファイルの設定が上書きされます。生データはそれを使って補正され、すでにキャップが設定されている記録データは再参照されます（記録された補正は取り消され、選択された補正が適用されます）。
* **重要**: 選択したキャップは、記録時に物理的に装着されていたキャップと一致している必要があります。センサーもソフトウェアも物理的なキャップを検出することはできません。キャップIDが一致しない場合、スペクトルに誤った補正が適用されます。

ファイルごとのドロップダウンメニューではなく、プロジェクト全体で**1つ**の制御項目が意図的に設けられています。この設定は、プロジェクト内のすべてのダウンウェルリング光源に適用されます。***

## アレイのアライメント

このセクションは、プロジェクト内の少なくとも1枚の画像に、LATTICEアレイが撮影時に付与するモジュール間アライメント変換 （XMP `Chloros:Alignment*` タグ）が付与されている場合にのみ表示されます。ここでは、アライメントタグが付与されている画像の数、基準となるカメラ（`REF` バッジ）、およびカメラごとの画像数の一覧が表示されます。

<!-- SCREENSHOT-NEEDED: Project Settings "Array Alignment" section for an imported LATTICE array capture set, showing the tagged-image count, the per-camera rows with the REF badge, and the three controls (Apply array alignment, Crop to common overlap, Resampling). -->

### アレイ位置合わせの適用

* **タイプ**: チェックボックス
* **デフォルト**: 有効（チェック済み）
* **保存形式**: `Processing → "Array alignment"`
* **説明**: 処理済みのすべてのプロダクト（デベイヤー済み／プレビュー／放射度／反射率／屈折率）を、撮影時にスタンプされた変換を使用して、アレイの共通基準ジオメトリにワープします。オフの場合、センサーごとのネイティブジオメトリでエクスポートされます。

### 共通オーバーラップ領域にトリミング

* **タイプ**: チェックボックス（*配列アライメントの適用*がオンの場合のみ有効）
* **デフォルト**: 有効（チェック入り）
* **保存形式**: `Processing → "Array alignment crop"`
* **説明**: アライメント済みのエクスポートデータを、すべてのカメラモジュールが共有する領域にトリミングし、すべてのバンドで同じフットプリントになるようにします。「オフ」の場合は、センサーのキャンバス全体が保持されます（ソースの外側は黒で塗りつぶされます）。

### リサンプリング

* **タイプ**: ドロップダウン選択（*配列アライメントを適用*がオンの場合にのみ有効）
* **オプション**: `Bilinear (smooth, default)`、`Nearest (preserve exact values)`、`Cubic (sharpest)`
* **デフォルト**: バイリニア
* **保存形式**: `Processing → "Array alignment interpolation"`
* **説明**: アライメントワープで使用される補間法。「最近傍法」は厳密な放射測定分析のためにソースの正確な値を保持します（ピクセル間の混合なし）。「双線形法」はマッピングや視覚的な用途に最適です。

これら3つのオプションは、頭部なしの設定として `chloros-cli process --array-alignment`、`--array-alignment-crop`、および `--array-alignment-interp {bilinear,nearest,cubic}` としても存在します。

***

## インデックス

これらの設定により、解析および可視化のためのマルチスペクトルインデックスを構成できます。

### インデックスの追加

* **タイプ**: 特殊なインデックス設定パネル
* **説明**: 画像処理中に計算するマルチスペクトル植生インデックス（NDVI

、NDRE

、EVI

など）を選択・設定できる対話型パネルを開きます。複数のインデックスを追加でき、それぞれに独自の可視化設定を指定できます。
* **利用可能なインデックス**: GUIのドロップダウンには**27** 事前定義されたマルチスペクトル指標式が含まれています（完全なリストについては、[マルチスペクトル指標式](multispectral-index-formulas.md)を参照してください。ここには、CLI

/SDK

`--indices` オプションでも受け入れられる名称も含まれています）。
* **機能**：
  * あらかじめ定義されたインデックス式から選択
  * カメラのフィルターチャンネルを式内のバンドスロットにドラッグ
  * 視覚化用のカラーグラデーション（LUT - ルックアップテーブル）の設定
  * 閾値とクリッピングモードの設定
  * カスタムインデックス式の作成
* **注**: シングルバンドのLATTICE M3Mモノクロカメラではインデックスは計算されません。1つのバンドのみでは、マルチバンドインデックスは未定義となります。Survey3

およびLATTICE M3Cは影響を受けません。



<!-- SCREENSHOT-NEEDED: Project Settings > Index section with one index added and expanded: the filter dropdown, the formula dropdown open showing preset names, the coloured channel circles above the rendered formula, and the "+ Add LUT" button below it. -->

追加する各インデックスは、その数式を数学式としてレンダリングし、バンドスロットごとに色付きの円が表示されます：赤 =Red

、緑 =Green

、青 =Blue

、オレンジ =Orange

、シアン =Cyan

、紫 =NIR

、マゼンタ = RE。 数式のすぐ上の行にある円をスロットにドラッグしてバインドします。バインドされたスロットをダブルクリックすると、バインドが解除されます。インデックスの計算は、数式が使用するすべてのスロットにチャンネルが割り当てられた時点で一度だけ行われます。

### カスタム数式（Chloros

+ 機能）

* **タイプ**: カスタム数式定義の配列
* **利用条件**: 対象となるChloros

+ サブスクリプションでのサインインが必要です。
* **説明**: バンド演算を使用して、カスタムマルチスペクトルインデックス数式を作成・保存できます。カスタム数式はプロジェクト設定とともに保存され、組み込みのインデックスと同様に使用できます。
* **作成方法**:
  1. インデックス設定パネルで、カスタム数式計算機を開きます。
  2. バンド名ではなく、**バンドスロット記号**を使用して式を入力します
  3. わかりやすい名前を付けて式を保存します。保存すると、式のドロップダウン一覧の下部に表示され、組み込みのプリセットとまったく同じように、カメラのチャンネルサークルをそのスロットにドラッグして割り当てることができます
* **数式の構文**：
  * バンドスロット：`x`、`y`、 `z`, `a`, `b`, `c` — ドラッグして実際のチャンネルにマッピングする6つの位置
  * オペレーター：`+`、`-`、`*`、 `/`、`^`、および `()`（グループ化用）
  * 関数：`sqrt()`、`log()`、`ln()`、`abs()`、 `sign()`、`log1p()`、`log2()`
* **なぜバンド名ではなく記号なのか**: `(y-x)/(y+x)` という形式で記述された数式は、どのカメラでも機能します。これは、ドラッグ＆ドロップによるマッピングによって、`y` がRGN

の 850 nm フィルターか、OCN

の 808 nm フィルター（NIR

）かを決定するためです。 組み込みのプリセットも同様に保存されます。全27個の正確な記号形式については、[マルチスペクトル指数式](multispectral-index-formulas.md)を参照してください。
* **使用可能な場所**：カスタム式はプロジェクト設定とともに保存され、 [インデックス/LUTサンドボックス](../image-viewer-gui/index-lut-sandbox.md) および処理工程で利用可能です。ただし、CLI

/SDK

`--indices`の名称リストでは**受け付けられません**。このリストは、22個の組み込みプリセット名の展開のみを目的としています。***

## エクスポート

これらの設定は、処理済み画像のエクスポート形式と品質を制御します。

### キャリブレーション済み画像形式

* **タイプ**：ドロップダウン選択
* **オプション**：
  * **TIFF

(16-bit)** - 非圧縮の16ビットTIFF

形式
  * **TIFF

(32-bit, Percent)** - 32ビット浮動小数点TIFF

形式（反射率値をパーセンテージで表現）
  * **PNG

（8ビット）** - 圧縮された8ビットPNG

形式
  * **JPG（8ビット）** - 圧縮された8ビットJPEG

形式
* **デフォルト**:TIFF

（16ビット）
* **説明**: 処理およびキャリブレーション済みの画像を保存するためのファイル形式を選択します。 エクスポートされた画像は、各カメラのフォルダ内のフォーマットごとのサブフォルダに保存されます （`tiff16`、`tiff32`、`png8`、`jpg8`）。各プロダクトにつき1つの`<Product>_Images/`フォルダが作成されます。エクスポートされたファイルは元のファイル名を保持します。プロダクトを識別するのはフォルダ名であり、ファイル名の拡張子ではありません。
* **推奨フォーマット**:
  * **TIFF

（16ビット）**：科学的な分析や専門的なワークフローに推奨されます。圧縮によるアーティファクトがなく、データの品質を最大限に維持します。マルチスペクトル分析やGISソフトウェアでのさらなる処理に最適です。
  * **TIFF

(32ビット、パーセント)**: 反射率値をパーセント (0～100%) で必要とするワークフローに最適です。放射測定において最高の精度を提供します。
  * **PNG

(8ビット)**: Webでの閲覧や一般的な可視化に適しています。 ロスレス圧縮によりファイルサイズは小さくなりますが、ダイナミックレンジは狭くなります。
  * **JPG（8ビット）**： ファイルサイズが最も小さく、プレビューやWeb表示にのみ最適です。非可逆圧縮を使用しているため、科学的な分析には適していません。
* **注**: LATTICEのラディアンスは、この設定にかかわらず、常に32ビット浮動小数点TIFF

としてエクスポートされます。

***

## プロジェクトテンプレートの保存

この機能を使用すると、現在のプロジェクト設定を再利用可能なテンプレートとして保存できます。

* **タイプ**：テキスト入力欄 + 保存ボタン
* **説明**：設定テンプレートのわかりやすい名前を入力し、保存アイコンをクリックしてください。 テンプレートには、現在のプロジェクト設定（ターゲット検出、処理オプション、インデックス、エクスポート形式）がすべて保存されるため、将来のプロジェクトで簡単に再利用できます。テンプレートは、プロジェクトの保存フォルダ内の「`Project Templates/`」フォルダに保存され、メインメニュー（*テンプレートの選択* / *テンプレートの保存* / *テンプレートのエクスポート*）から選択またはエクスポートすることもできます。
* **利用例**:
  * さまざまなカメラシステム（RGB

、マルチスペクトル、NIR

）用のテンプレートを作成する
  * 特定の作物種類や解析ワークフロー向けの標準設定を保存する
  * チーム全体で統一された設定を共有する
* **使用方法**:
  1. 必要なプロジェクト設定をすべて行います
  2. テンプレート名を入力します（例：「RedEdge

Survey3

NDVI

Standard」）
  3. 保存アイコンをクリックします
  4. これで、新しいプロジェクトを作成する際にこのテンプレートを読み込めるようになります

***

## プロジェクト保存フォルダ

この設定では、新しいプロジェクトをデフォルトで保存する場所を指定します。

* **タイプ**: ディレクトリパスの表示 + 編集ボタン
* **デフォルト (Windows

)**: `C:\Users\[Username]\Chloros Projects`
* **デフォルト (Linux

)**: `~/Chloros Projects`
* **説明**: 新しいChloros

プロジェクトが作成される現在のデフォルトディレクトリを表示します。 編集アイコンをクリックして、別のディレクトリを選択してください。この上書き設定は、`~/.chloros/working_directory.txt` ファイル（Windows

では `C:\Users\<Username>\.chloros\working_directory.txt`）に 1 行のテキストとして保存されます。そのファイルが存在しない場合、または存在しなくなったパスが指定されている場合、Chloros

は上記のデフォルト設定にフォールバックします。CLI

は同じファイルを読み書きするため、`chloros-cli`とGUIはプロジェクトの保存場所について常に一致しています。
* **プロジェクトテンプレート**は、このディレクトリ内の`Project Templates/`サブフォルダに格納されています。
* **変更すべき場合**:
  * チームでの共同作業を行う場合は、ネットワークドライブに設定します
  * 大規模なデータセットの場合は、より容量の大きいドライブに変更します
  * 年、クライアント、またはプロジェクトの種類ごとに、異なるフォルダにプロジェクトを整理します
* **注**：この設定の変更は、新規プロジェクトにのみ影響します。既存のプロジェクトは元の場所にそのまま残ります。***

## 設定の永続性

Chloros

のプロジェクトは**フォルダ**です。すべてのプロジェクト設定は、その中の`project.json`に保存されます。 接続されたハードウェア情報は、`cameras.json`および`sensors.json`に併せて保存されるため、プロジェクトを再開すると、カメラや光センサーも自動的に再接続されます。プロジェクトを再開すると、 すべての設定が、終了時の状態とまったく同じように復元されます。保存されたプロジェクトは、`chloros-cli project` またはSDK

の `open_project` を使用して、ヘッドレスでも操作可能です。

### 設定の階層構造

設定は次の順序で適用されます：

1. **システムのデフォルト設定** -Chloros

で定義された組み込みのデフォルト設定2. **テンプレート設定** - プロジェクト作成時にテンプレートを読み込んだ場合
3. **保存されたプロジェクト設定** - プロジェクトファイルとともに保存された設定
4. **手動による調整** - 現在のセッション中に行った変更

### 設定と画像処理

処理設定は、処理の実行が開始された際に読み込まれます。設定を変更しても、すでにディスク上に存在する出力データには遡及的に変更は反映されません。新しい設定を適用するには、処理を再度実行してください。以下の設定は、処理に一切影響を与えません：

* 画像サムネイルの解像度（表示専用）
* プロジェクトテンプレートの保存
* プロジェクトフォルダの保存

***

## 構成キーのリファレンス

自動化（CLI

`--config`、SDK

`configure`、または `project.json` を直接読み込む場合）については、 `Project Settings` 配下にある正確なキーは以下の通りです：

| キーパス | タイプ | デフォルト |
| --- | --- | --- |
| `Display → Image Thumbnail Resolution` | `"512" \| "1024" \| "2048" \| "full"` | `"512"` |
| `Target Detection → Minimum calibration sample area (px)` | 0～10000 | `25` |
| `Target Detection → Minimum Target Clustering (0-100)` | 0～100の数値 | `60` |
| `Processing → Vignette correction` | ブール値 | `true` |
| `Processing → Reflectance calibration / white balance` | bool | `true` |
| `Processing → Export sensor response` | bool | `true` |
| `Processing → Export vignette corrected` | bool | `true` |
| `Processing → Export debayered` | bool | `true` |
| `Processing → Export preview` | bool | `true` |
| `Processing → Export radiance` | bool | `true` |
| `Processing → Export reflectance` | bool | `true` |
| `Processing → Array alignment` | bool | `true` |
| `Processing → Array alignment crop` | bool | `true` |
| `Processing → Array alignment interpolation` | `"Bilinear" \| "Nearest" \| "Cubic"` | `"Bilinear"` |
| `Processing → Debayer method` | `"Standard (Fast, Medium Quality)" \| "Texture Aware (Slow, Highest Quality)"` | 標準 |
| `Processing → Minimum recalibration interval` | 数値 0～3600 | `0` |
| `Processing → Light sensor timezone offset` | 数値 -12..12 | `0` |
| `Processing → Apply PPK corrections` | ブール値 | `false` |
| `Processing → DAQ cap id` | キャッププロファイルID または `"auto"` | `"auto"` |
| `Processing → Target reflectance source` | `"auto" \| "target" \| "daq"` | `"auto"` |
| `Index → Add index` | インデックス設定のリスト | `[]` |
| `Export → Calibrated image format` | `"TIFF (16-bit)" \| "TIFF (32-bit, Percent)" \| "PNG (8-bit)" \| "JPG (8-bit)"` | `"TIFF (16-bit)"` |

`Array alignment` キーは、Array Alignment セクションが最初にレンダリングされる際、またはオートメーション呼び出しによって設定された際に書き込まれます。これらが存在しない場合、パイプラインは上記に示されたのと同じ値 （`true`、`true`、双線形）と同じ値を使用するため、これらのキーがないプロジェクト.jsonは、キーがあるプロジェクトと全く同じ動作をします。

### 設定パネルにコントロールがない状態で `project.json` に保存されたキー

これらは同じ `Project Settings` ツリー下にあり、処理によって読み込まれますが、サイドバーにはそれに対応するウィジェットは表示されません：

| キーパス | タイプ | デフォルト | 設定元 |
| --- | --- | --- | --- |
| `Processing → LATTICE input level` | `"auto" \| "raw" \| "debayered" \| "processed"` | `"auto"` | `chloros-cli process --input-level`、SDK

`input_level=`。LATTICE入力TIFFの解釈方法を上書きします。`auto`は、各ファイルの`Chloros:ProcessingLevel` XMPタグとチャンネル数から推測します。Survey3

の`.raw`キャプチャでは無視されます。意図的にGUI設定にはしていません — 通常のケースでは「auto」が正しい設定です。 |
| `Processing → Target reflectance dir` | パス文字列 | `""` | `chloros-cli process --target-reflectance-dir`、 またはプロジェクトのターゲットAPI

|
| `Processing → Target reflectance config` | カメラのシリアル番号をキーとする辞書 | `{}` | フレーム内ターゲットの登録 （モード `fixed_block` / `fixed_strip` / `aruco`） |
| `Processing → DAQ-U log path` | パス文字列 | `""` |SDK

`process_folder(daq_log_path=…)`。`.daq`の録画ファイルまたはそのフォルダを指す |
| `Target Detection → Minimum calibration target squares` | 数値 | `4` | 従来のデフォルト設定。制御なし、CLI

フラグなし |
| `UI → Grid thumbnail size` | 番号 | `160` | 画像グリッド独自のサムネイルズームスライダー |

2つのビューア設定は、処理設定ではなく表示状態であるため、`Project Settings`の外側、**`project.json`の最上位**に保存されています：

| キーパス | タイプ | デフォルト | 設定元 |
| --- | --- | --- | --- |
| `viewer_display → gsd_bin` | 整数 1–256 | `1` | 画像タブの GSD (px) コントロール — [画像を全画面で開く](../image-viewer-gui/opening-an-image-full-screen.md) を参照 |

***

## ベストプラクティス

1. **デフォルト設定から始める**：デフォルト設定は、ほとんどのMAPIR

カメラシステムや一般的なワークフローで問題なく機能します。
2. **テンプレートを作成する**：特定のワークフローやカメラに合わせて設定を最適化したら、プロジェクト間で一貫性を保つためにテンプレートとして保存してください。
3. **本格的な処理の前にテストを行う**：新しい設定を試す際は、データセット全体を処理する前に、少数の画像でテストを行ってください。
4. **設定内容を記録する**：カメラシステム、処理の種類、および用途がわかるような説明的なテンプレート名を使用してください（例：「Survey3

_RGB_NDVI_Agriculture」）。
5. **エクスポート形式の選択**：最終的な用途に応じて、エクスポート形式を選択してください：
   * 科学的な分析 →TIFF

(16ビットまたは32ビット)
   * GIS処理 →TIFF

(16ビット)
   * 簡易な可視化 →PNG

(8ビット)
   * Webでの共有 → JPG (8ビット)

***

Chloros

のマルチスペクトル指数に関する詳細については、[マルチスペクトル指数の計算式](multispectral-index-formulas.md)のページを参照してください。
