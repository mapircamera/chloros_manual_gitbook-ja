# GUI：プロジェクト

Chloros では、後で再開できるプロジェクトを作成できます。プロジェクトとは、（プロジェクトフォルダ内の）単なるフォルダであり、以下が含まれます：

* `project.json` — プロジェクト設定、ファイル一覧、および表示設定
* `cameras.json` — プロジェクトが開かれている間に接続されたカメラおよびアレイ、およびそれらの設定
* `sensors.json` — プロジェクトが開かれている間に接続された DAQ 光センサー、およびカメラ↔センサーの紐付け
* キャプチャデータ、`.daq` 記録データ、および処理済み出力フォルダ

独自のプロジェクトファイル形式は存在しません。フォルダとその中の JSON ファイル自体がプロジェクトとなります。これにより、プロジェクトのコピー、アーカイブ、および [CLI](CLI.md) や [Python SDK](api-python-sdk.md) から簡単に実行できます。

## 新規プロジェクト

<figure><img src=".gitbook/assets/image (26).png" alt=""><figcaption></figcaption></figure>メインメニューから「新規プロジェクト」を選択し、プロジェクトに一意の名前を入力してください。

プロジェクトテンプレートを保存している場合、名前入力欄の下に**「テンプレートの選択」**ドロップダウンが表示されます。これを選択すると、そのテンプレートの設定に基づいて新しいプロジェクトが開始されます。 テンプレートは [プロジェクト設定](project-settings/project-settings.md) から保存します。「プロジェクトテンプレートの名前」フィールドに名前を入力し、保存アイコンをクリックしてください。

## プロジェクトを開く

<figure><img src=".gitbook/assets/v120-open-project.jpg" alt=""><figcaption><p>「プロジェクトを開く」には、プロジェクトフォルダ内のすべてのプロジェクトが一覧表示され、下部に「<strong>プロジェクトフォルダを開く</strong>」があります</p></figcaption></figure>「プロジェクトを開く」を選択すると、プロジェクトフォルダ内の既存のプロジェクト一覧が表示されます。プロジェクトが存在しない場合、サブサイドメニューは開きません。 上の写真には、GUIで作成されたいくつかのプロジェクト（t1、t2、t3）が一覧表示されています。「DATE\_TIME」という名前のプロジェクトは、CLIがデフォルトのプロジェクト命名規則を使用して作成したものです。任意のプロジェクト名をクリックすると、そのプロジェクトが開きます。

「プロジェクトフォルダを開く」ボタンをクリックすると、お使いのコンピュータのファイルエクスプローラーがプロジェクトのパスで開きます。プロジェクトのパスは、[プロジェクト設定](project-settings/project-settings.md)で調整できます。

プロジェクトを最後に開いてから、ソース画像ファイルが移動または削除された場合、Chlorosは空白のグリッドを開く代わりに、どのファイルが欠落しているかを正確に一覧表示するダイアログを表示します。

## プロジェクトの複製

プロジェクトを開いているときに利用可能です。「プロジェクトの複製」を選択すると、現在のプロジェクトが新しい名前でコピーされます（Chlorosは次に利用可能な名前（例：「MyProject (2)」）を提案します）。複製されたプロジェクトは直ちに開かれます。

## ファイルの追加

プロジェクトを開いた後、メインメニューから「ファイルの追加」を選択すると、個々の画像ファイルを現在のプロジェクトに追加できます。これはファイルブラウザの追加機能と同じですが、利便性のためメインメニューから直接アクセスできます。

## フォルダの追加

プロジェクトを開いた後、メインメニューから「フォルダの追加」を選択すると、画像を含むフォルダを現在のプロジェクトに追加できます。一度に複数のフォルダを選択可能です。重複するファイルは無視されます。

## 処理の開始／停止

プロジェクトにファイルが追加されると、メインメニューに「処理を開始」が表示されます。これは、上部ヘッダーの再生／開始ボタンをクリックするのと同じ操作です。処理中は、メニュー項目が「処理を停止」に変わり、パイプラインを中断できるようになります。

## カメラへの接続 / 光センサーへの接続

メインメニューの下部には、プロジェクトが開いているかどうかにかかわらず利用可能な 2 つのハードウェアショートカットがあります：

* **カメラへの接続** — [カメラタブ](lattice/) を開き、LATTICE カメラまたはアレイを接続します。
* **ライトセンサーに接続** — [ライトセンサータブ](daq/) を開き、DAQ ライトセンサーを接続します。

プロジェクトが開いている状態でハードウェアを接続すると、その設定はプロジェクトに保存されます（以下を参照）。プロジェクトが開いていない場合、接続はセッション限定となります。

{% hint style="info" %}
「ファイルの追加」、「フォルダの追加」、「処理の開始/停止」のメニュー項目は、プロジェクトが開かれており、ファイルが追加されている場合にのみ表示または有効になります。これらは、ファイルブラウザのサイドバーやヘッダーのボタンからも利用可能な操作へ素早くアクセスできるようにするものです。
{% endhint %}

## プロジェクトはハードウェアの設定を記憶します

バージョン 1.2.0 の新機能：プロジェクトが開かれている間、接続されたハードウェアの設定が保持されます。 カメラおよびアレイ（各カメラごとの設定、名前、色、グリッドレイアウトを含む）は `cameras.json` に、光センサー（名前、色、カメラへのバインドを含む）は `sensors.json` に — 作業中に自動的に保存されます。

プロジェクトを**再オープン**した際、Chlorosは直ちにハードウェアにアクセスしません。各ハーフは、それを所有するタブを初めて開いた際に再接続されます：

* **カメラ**タブを開くと、保存されたカメラとアレイが再接続され、保存された設定が再適用されます。
* **光センサー**タブを開くと、保存されたDAQセンサーが再接続されます。

この仕組みにより、画像の閲覧やエクスポートのみを目的としてプロジェクトを開いた場合でも、カメラがストリーミング状態になることはありません。タブを開いた際に保存されたデバイスが見つからない場合、どのデバイスが利用できないかを示すダイアログが表示されるため、再接続または削除を行うことができます。

## プロジェクト内のDAQ記録と.daqファイル

* プロジェクトが開いている間に作成された`.daq`記録（**「Light Sensors」**タブから、またはキャプチャ中に作成されたもの）は、**自動的にプロジェクトに追加**されます。
* インポートされた `.daq` ファイルおよびすべてのプロジェクト記録は、[プロジェクト設定](project-settings/project-settings.md) の **DAQ 光センサー** セクションに一覧表示され、それぞれにキャップ補正プロファイルが設定されています。
* 処理中、プロジェクトの `.daq` ファイルは、反射率プロダクト用の下向きの照明として使用されます — [出力画像フォーマット](output-image-formats.md) を参照してください。

## 保存済みプロジェクトのヘッドレス実行

保存されたプロジェクトは、GUI を使用せずに実行できます：

* **CLI**: `chloros-cli project open / connect / capture / sensor / align / run` はプロジェクトフォルダのパスに基づいて動作します — [CLI リファレンス](reference/cli-reference.md)を参照してください。
* **SDK**: `chloros_sdk.open_project(path)`はプロジェクトハンドルを返します。 `connect_all()`は、保存されているすべてのカメラとセンサーを、保存された設定とともにオンライン状態にします — [SDK リファレンス](reference/sdk-reference.md)を参照してください。
